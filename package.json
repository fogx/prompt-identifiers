{
  "name": "prompt-identifiers",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.2",
    "@types/jest": "^30.0.0",
    "@types/node": "^26.5.1",
    "prettier": "^3.9.6"
  }
}
