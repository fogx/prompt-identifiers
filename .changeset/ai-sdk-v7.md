---
"prompt-identifiers-ai-sdk": minor
---

**Breaking:** Support AI SDK v7. The middleware now implements `LanguageModelV4Middleware` (`specificationVersion: "v4"`) and requires `ai@>=7` and `@ai-sdk/provider@>=4`. Stay on 0.2.x for AI SDK v6.
